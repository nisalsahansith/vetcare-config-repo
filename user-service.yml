server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://10.152.0.3:5432/vetcare
    username: postgres
    password: 2003Nisal#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://10.10.0.4:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"